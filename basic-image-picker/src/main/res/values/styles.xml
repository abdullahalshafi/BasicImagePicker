<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="BasicImagePickerTheme.Transparent" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>

    <style name="ef_BaseTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionModeOverlay">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ef_AppTheme" parent="ef_BaseTheme">
        <!-- Hardcoded so this theme has a usable fallback even when consumers haven't overridden
             basic_image_picker_colorPrimary (which is now a sentinel value — see colors.xml). -->
        <item name="colorPrimary">#3F51B5</item>
        <item name="colorPrimaryDark">#303F9F</item>
        <item name="colorAccent">@color/ef_colorAccent</item>
    </style>

    <style name="ef_CustomToolbarTheme" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <!-- Hardcoded so the toolbar fallback (no resource override, no theme-derived
             programmatic color) is still visible. The override-detection resource
             basic_image_picker_colorTextPrimary is a sentinel and must NOT be used for theme
             defaults — see colors.xml. Programmatic toolbar.setTitleTextColor still wins. -->
        <item name="android:textColorPrimary">#FFFFFF</item>
        <item name="actionMenuTextColor">#FFFFFF</item>
    </style>

</resources>